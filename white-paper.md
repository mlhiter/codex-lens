项目名称： Codex Lens

1. 问题陈述 (The Problem)
现代编程环境中，VS Code 的原生悬浮提示（IntelliSense）虽然能提供类型定义和 JSDoc 注释，但往往缺乏实际应用层面的信息。开发者，尤其是面对一个不熟悉的库或复杂的 API 时，仍然需要频繁地切换到浏览器搜索文档、查找用法示例，这个过程会打断心流，降低开发效率。

2. 解决方案 (The Solution)
Codex Lens 是一款 VS Code 扩展，旨在彻底革新代码悬浮提示体验。它通过集成强大的大型语言模型（LLM），在用户悬停于任何 API、函数或变量上时，动态地提供一个增强的、内容丰富的提示窗口。这个窗口不仅包含传统的类型信息，更提供了由 AI 生成的：

清晰的功能解释： 用自然语言说明这个 API 是做什么的。

实用的代码示例： 展示该 API 在真实场景中的典型用法。

复杂参数解析： 对复杂的配置对象或参数进行拆解说明。

3. 目标用户
所有使用 VS Code 进行 JavaScript/TypeScript 开发的工程师，从初学者到资深专家。

4. MVP 核心范围 (Scope)
为了快速验证核心想法，MVP 阶段我们将严格限制功能范围：

【包含】支持语言： 仅限 JavaScript 和 TypeScript。

【包含】核心功能： 实现自定义的 HoverProvider，当用户悬停时触发。

【包含】LLM 集成： 调用一个外部 LLM API（我们将以 Claude 为目标）来生成内容。

【包含】配置方式： 要求用户在 VS Code 的 settings.json 中手动配置自己的 LLM API Key。

【包含】UI 展示： 使用 VS Code 原生的悬浮窗口，通过 Markdown 格式展示 LLM 返回的结果。

【不包含】高级功能： 结果缓存、自定义 Prompt、UI 设置界面、支持多种 LLM 模型、多语言支持等功能，均不在 MVP 范围。

5. 成功的标准
MVP 的成功标准很简单：用户能够成功配置自己的 API Key，并在悬停于一个 JS/TS 函数上时，看到由 LLM 生成的包含“解释”和“代码示例”的悬浮窗口。响应时间在可接受范围内（例如 3-5 秒内）。